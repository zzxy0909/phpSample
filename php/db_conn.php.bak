<?php
$user = 'DB ID';
$password = 'DB PW';
$db = 'DB';
$host = 'localhost';
$port = 8889;

$link = mysql_connect(
   "$host:$port", 
   $user, 
   $password
) or die("접속 에러");

$db_selected = mysql_select_db(
   $db, 
   $link
) or die("연결 에러");
?>
